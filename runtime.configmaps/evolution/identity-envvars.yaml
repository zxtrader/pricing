# kubectl --namespace cexpay-evolution-admin get --output=yaml ConfigMap/identity-envvars
# kubectl --namespace cexpay-evolution-admin get --output=json ConfigMap/identity-envvars | jq -r ".data" | yq eval -P | sed 's/^/  /'
# kubectl --namespace cexpay-evolution-admin apply --filename=runtime.configmaps/evolution/identity-envvars.yaml

apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-envvars
  namespace: cexpay-evolution-admin
