apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Chart.Name }}-echo{{ template "suffix" . }}
  name: {{ .Chart.Name }}-echo{{ template "suffix" . }}
spec:
  ports:
    - port: 8443
      name: echo
      protocol: TCP
      targetPort: 8443
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}-echo{{ template "suffix" . }}
