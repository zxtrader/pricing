apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Chart.Name }}-dashboardserve{{ template "suffix" . }}
  name: {{ .Chart.Name }}-dashboardserve{{ template "suffix" . }}
spec:
  ports:
    - port: 8080
      name: dashboardserve
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}-dashboardserve{{ template "suffix" . }}
