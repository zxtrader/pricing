apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Chart.Name }}-dashboard{{ template "suffix" . }}
  name: {{ .Chart.Name }}-dashboard{{ template "suffix" . }}
spec:
  ports:
    - port: 8445
      name: dashboard
      protocol: TCP
      targetPort: 8445
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}-dashboard{{ template "suffix" . }}
