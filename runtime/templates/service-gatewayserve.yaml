apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Chart.Name }}-gatewayserve{{ template "suffix" . }}
  name: {{ .Chart.Name }}-gatewayserve{{ template "suffix" . }}
spec:
  ports:
    - port: 8080
      name: gatewayserve
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}-gatewayserve{{ template "suffix" . }}
