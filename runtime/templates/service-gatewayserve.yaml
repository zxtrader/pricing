apiVersion: v1
kind: Service
metadata:
  labels:
    app: gateuiserve{{ template "suffix" . }}
  name: gateuiserve{{ template "suffix" . }}
spec:
  ports:
    - port: 8080
      name: gateuiserve
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
  selector:
    app: gateuiserve{{ template "suffix" . }}
