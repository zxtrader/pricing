apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: {{ template "prefix" . }}api
  name: {{ template "prefix" . }}api
spec:
  replicas: {{ .Values.application.api.replicas | default 0 }}
  strategy:
    type: RollingUpdate
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        app: {{ template "prefix" . }}api
    spec:
      # initContainers:
      # - name: secret-copier
      #   image: busybox:latest
      #   imagePullPolicy: Always
      #   command:
      #   - sh
      #   - -c
      #   args:
      #   - 'echo "Copying ssl and secrets files into temporary emptyDir-based volume (to be able to erase sensetive data after read it)."
      #     && cp /etc/cexiolabs/cryptopay.api/ssl/* /tmp/cexiolabs/cryptopay.api/ssl/
      #     && cp /etc/cexiolabs/cryptopay.api/secrets/* /tmp/cexiolabs/cryptopay.api/secrets/'
      #   volumeMounts:
      #   - name: secret-certificate
      #     mountPath: /etc/cexiolabs/cryptopay.api/ssl/
      #   - name: secret-files
      #     mountPath: /etc/cexiolabs/cryptopay.api/secrets/
      #   - name: secret-certificate-copy
      #     mountPath: /tmp/cexiolabs/cryptopay.api/ssl/
      #   - name: secret-files-copy
      #     mountPath: /tmp/cexiolabs/cryptopay.api/secrets/
      containers:
      - name: api
        image: {{ .Values.application.api.serviceImage }}:{{ .Values.application.api.tag }}
{{- if .Values.application.api.command }}
        command:
{{ range .Values.application.api.command }}
        - {{ . }}
{{ end }}
{{- end }}
{{- if .Values.application.api.args }}
        args:
{{ range .Values.application.api.args }}
        - {{ . }}
{{ end }}
{{- end }}
        # envFrom:
        # - configMapRef:
        #     name: api
        # envFrom:
        # - secretRef:
        #     name: api
        env:
        - name: CRYPTOPAY_ENVIRONMENT
          value: {{ .Values.environment }}
{{- if .Values.application.api.env }}
{{ range $key, $value := .Values.application.api.env }}
        - name: "{{ $key }}"
          value: "{{ $value }}"
{{ end }}
{{- end }}
        # lifecycle:
        #   postStart:
        #     exec:
        #       command: ["rm", "/etc/cexiolabs/cryptopay.api/ssl/tls.key"]
        livenessProbe:
          httpGet:
            path: /info
            port: 8443
            scheme: HTTPS
          initialDelaySeconds: 180
          periodSeconds: 60
        ports:
        - containerPort: 8443
          protocol: TCP
        # readinessProbe:
        #   httpGet:
        #     path: /info
        #     port: 8443
        #     scheme: HTTPS
        #   initialDelaySeconds: 60
        #   periodSeconds: 30
        resources:
          limits:
            cpu: 512m
            memory: 512Mi
          requests:
            cpu: 128m
            memory: 64Mi
        # # Will be available with Kubernetes 1.20
        # startupProbe:
        #   httpGet:
        #     path: /info
        #     port: 8443
        #   failureThreshold: 60
        #   periodSeconds: 5
        volumeMounts:
        # - name: config-files
        #   mountPath: /etc/cexiolabs/cryptopay.api/
        #   readOnly: true
        - name: legacy-cpservice-config
          mountPath: /etc/cexiolabs/cryptopay.api/legacy-cpservice-config/
          readOnly: true
        - name: secret-certificate
          mountPath: /etc/cexiolabs/cryptopay.api/ssl/
      #   - name: secret-certificate-copy
      #     mountPath: /tmp/cexiolabs/cryptopay.api/ssl/
        - name: secret-files
          mountPath: /etc/cexiolabs/cryptopay.api/secrets/
      #   - name: secret-files-copy
      #     mountPath: /tmp/cexiolabs/cryptopay.api/secrets/
      volumes:
      - name: legacy-cpservice-config
        configMap:
          defaultMode: 420
          name: cpservice
      # - name: config-files
      #   configMap:
      #     name: api-files
      - name: secret-certificate
        secret:
          secretName: {{ template "prefix" . }}api-certificate
      - name: secret-files
        secret:
          secretName: api
      - name: secret-certificate-copy
        emptyDir: {}
      - name: secret-files-copy
        emptyDir: {}
