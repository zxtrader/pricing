apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo{{ template "suffix" . }}
  name: demo{{ template "suffix" . }}
spec:
  ports:
    - port: 8080
      name: demo
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
  selector:
    app: demo{{ template "suffix" . }}
