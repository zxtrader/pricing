apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: {{ .Chart.Name }}-dashboarduiserve{{ template "suffix" . }}
  name: {{ .Chart.Name }}-dashboarduiserve{{ template "suffix" . }}
spec:
  replicas: {{ .Values.application.dashboarduiserve.replicas | default 2 }}
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-dashboarduiserve{{ template "suffix" . }}
    spec:
      containers:
      - name: dashboarduiserve
        image: {{ .Values.application.dashboarduiserve.serviceImage }}:{{ .Values.application.dashboarduiserve.tag }}
{{- if .Values.application.dashboarduiserve.command }}
        command:
{{ range .Values.application.dashboarduiserve.command }}
        - {{ . }}
{{ end }}
{{- end }}
{{- if .Values.application.dashboarduiserve.args }}
        args:
{{ range .Values.application.dashboarduiserve.args }}
        - {{ . }}
{{ end }}
{{- end }}
        volumeMounts:
        - name: secret-certificate
          mountPath: /etc/cexiolabs/cryptopay/ssl
          readOnly: true
        env:
        - name: RUNTIME
          value: {{ .Values.environment }}
{{- if .Values.application.dashboarduiserve.env }}
{{ range $key, $value := .Values.application.dashboarduiserve.env }}
        - name: "{{ $key }}"
          value: "{{ $value }}"
{{ end }}
{{- end }}
        ports:
        - containerPort: 8080
          protocol: TCP
        # livenessProbe:
        #   httpGet:
        #     path: /livenessProbe
        #     port: 9090
        #     scheme: HTTP
        #   initialDelaySeconds: 10
        #   periodSeconds: 10
        # readinessProbe:
        #   httpGet:
        #     path: /readinessProbe
        #     port: 9090
        #     scheme: HTTP
        #   initialDelaySeconds: 10
        #   periodSeconds: 10
      volumes:
      - name: secret-certificate
        secret:
          secretName: {{ .Chart.Name }}-dashboarduiserve{{ template "suffix" . }}-certificate
